{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "run-server": "yarn install && yarn run build && pm2 start pm2.config.js --no-daemon",
    "run-dev-server": "yarn install && yarn run clean && ts-node-dev --respawn --transpileOnly ./src/app.ts",
    "build": "yarn run clean && yarn run copy-assets && tsc",
    "clean": "rm -rf ./build/*",
    "copy-assets": "cp -R ./src/views ./build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "ts-node-dev": "^1.0.0-pre.44",
    "typescript": "^3.7.2"
  }
}
